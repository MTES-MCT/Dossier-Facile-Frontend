{
  "name": "e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for DossierFacile",
  "scripts": {
    "test": "cypress run",
    "cy:open": "cypress open --browser electron --e2e"
  },
  "dependencies": {
    "@types/cypress": "^1.1.3",
    "cypress": "^12.3.0"
  }
}
